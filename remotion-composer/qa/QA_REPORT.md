# QA — deterministic render evidence (Worker B, PR #3)

All render artifacts live under `remotion-composer/qa/out/` and input media under
`remotion-composer/public/qa-*` — BOTH gitignored (`qa/.gitignore` → `out/`;
`.gitignore` → `remotion-composer/public/*`). This report is text-only and commits;
the binaries it references are regenerable with the commands below. No paid
generation — inputs are synthesized locally with FFmpeg.

## Reproduce

```bash
cd remotion-composer && npm ci

# Deterministic inputs (free, local):
ffmpeg -y -f lavfi -i "sine=frequency=440:duration=8" -f lavfi -i "sine=frequency=554:duration=8" \
  -filter_complex "[0][1]amix=inputs=2:normalize=0,volume=0.5[a]" -map "[a]" -c:a libmp3lame -q:a 4 public/qa-tone.mp3
ffmpeg -y -f lavfi -i "gradients=s=1920x1080:c0=0x0f1b3a:c1=0x3a1e50:c2=0x123a3f:c3=0x2a1230:x0=200:y0=120:x1=1720:y1=960:nb_colors=4" \
  -frames:v 1 public/qa-photo.png

# Serve inputs so the headless render browser can fetch them via absolute URLs:
python3 -m http.server 4760 --bind 127.0.0.1 --directory public &

# Render with the SAFE PINNED wrapper ONLY (no npx / no PATH remotion / no --version;
# the wrapper realpath-resolves node_modules/.bin/remotion and verifies containment):
node scripts/remotion-cli.mjs render src/index.tsx TimelineFrame qa/out/qa_render.mp4 \
  --props=qa/props_render.json --log=error

# 5-minute metadata invariant, no full encode:
node scripts/remotion-cli.mjs compositions src/index.tsx --props=qa/props_9000.json
#   → TimelineFrame  30  1920x1080  9000 (300.00 sec)
```

The props (`qa/props_render.json`) intentionally play a title, a lower-third, a
caption and TWO audio tracks simultaneously to exercise peak-content overlap.

## Artifact (absolute paths)

- Video: `/Users/jonayedahamed/Maestro/worktrees/669a5386-f37b-4c6f-a712-b12e8221e54d/6b0aea9b-e5c9-4a22-b264-57aeba381efb/remotion-composer/qa/out/qa_render.mp4`
- SHA-256: `315d50cbe5d4db65d52eeab71f7fd05dc45ee858c8e391b67fa2e7bae6912403`
- Size: 2,096,109 bytes
- Raw ffprobe JSON: `.../remotion-composer/qa/out/ffprobe.json` (regenerated by `ffprobe -print_format json -show_format -show_streams`)

### Raw ffprobe (`-print_format json -show_format -show_streams`, key fields)

```json
{
  "streams": [
    { "index": 0, "codec_name": "h264", "profile": "High", "codec_type": "video",
      "width": 1920, "height": 1080, "display_aspect_ratio": "16:9",
      "pix_fmt": "yuvj420p", "r_frame_rate": "30/1", "avg_frame_rate": "30/1",
      "duration": "8.000000", "nb_frames": "240", "bit_rate": "1766411" },
    { "index": 1, "codec_name": "aac", "profile": "LC", "codec_type": "audio",
      "sample_fmt": "fltp", "sample_rate": "48000", "channels": 2,
      "channel_layout": "stereo", "duration": "8.042667", "nb_frames": "377",
      "bit_rate": "317374" }
  ],
  "format": { "format_name": "mov,mp4,m4a,3gp,3g2,mj2", "duration": "8.042667",
    "size": "2096109", "bit_rate": "2084988", "probe_score": 100,
    "tags": { "encoder": "Lavf61.7.100", "comment": "Made with Remotion 4.0.484" } }
}
```

### Probes

- Decoded video frame count (`ffprobe -count_frames`): **240** (= 8 s × 30 fps, exact)
- `blackdetect` (d=0.05, pic_th=0.98): **no black frames**
- `silencedetect` (-50 dB, d=0.3): **no silence**
- `volumedetect`: mean **-27.1 dB**, max **-20.9 dB**
- `ebur128` integrated loudness: **-24.8 LUFS**
- Metadata invariant: `TimelineFrame` reports **9000 (300.00 sec)** with the 300 s props.

## Frame-by-frame visual inspection (absolute paths)

Every frame inspected by eye — no clipping, overlap, placeholder, missing media, or
garbled text; all elements inside title-safe zones (see `src/layout.ts`, asserted in
`src/layout.test.ts`).

- `.../qa/out/frame_20.png` — pre-title: gradient media, `IMAGE` badge (top-left),
  lower-third card, two stacked audio rows (`♪ MUSIC 40%`, `▶ NARRATION 100%`). Clean.
- `.../qa/out/frame_60.png` — title entering: title/subtitle appear centered while the
  lower-third + caption + audio rows hold their zones. No collision.
- `.../qa/out/frame_120.png` — **PEAK overlap**: badge + `PREVIEW = RENDER` title +
  subtitle + lower-third + caption + `MUSIC 40%` + `NARRATION 100%` all on screen,
  each in its own zone, zero overlap, bounded/legible percentages.
- `.../qa/out/frame_180.png` — sustained peak overlap, same clean stacking.
- `.../qa/out/frame_232.png` — outro: title/caption ended; badge + lower-third + audio
  rows remain; music waveform dims as it fades out. Clean.
